# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage

# Production
dist
build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Turbo
.turbo

# IDEs
.vscode/*
!.vscode/extensions.json
.idea
*.swp
*.swo
*~

# OS
Thumbs.db
.vercel

# Backups
*.bak
.harness/backups/

# Android 릴리스 서명 (절대 커밋 금지 — 분실 시 앱 업데이트 불가, 별도 백업 필수)
*.keystore
keystore.properties

# Firebase 서비스 계정 키 (서버 발송 비밀 — 절대 커밋 금지)
firebase-service-account.json

# Integration test runtime artifacts (the test script itself IS committed)
test-runtime/backend.log
test-runtime/backend.pid

# Local backups (bundles/snapshots) — never commit
.backups/
